No configuration is needed. The "Calculated Gender" property is added to
the shared contact Properties definition on install; the AI Fields cron
requires an OpenAI API key to be configured (see the *ai* module) to
actually fill in values.
