# from odoo import api, fields, models

# _MAIN_GUEST_CHECK_SELECTION = [("na", "NA"), ("ok", "OK"), ("invalid", "Invalid")]


# class XGuestsLine(models.Model):
# _inherit = "x_guests_line"

# ACHTUNG: thies does get ignored
# all fields need to be added in data/ir_model_fields.xml to be visible in the UI, otherwise they are ignored
# x_testibus = fields.Char()
