Adds a "GOG Testing" page to the company form with a search term field and
a button that searches Gmail via the `gog` CLI, showing the first 10
matching messages. Used to verify the `gog` CLI / credential setup shared
by `insert_from_mail` and `invoice_from_mail` without going through their
booking/reconciliation flows.
