<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record
        id="resource_resource_view_form_inherit_camping_map_booking"
        model="ir.ui.view"
    >
        <field name="name">resource.resource.form.inherit.camping.map.booking</field>
        <field name="model">resource.resource</field>
        <field name="inherit_id" ref="resource.resource_resource_form" />
        <field name="arch" type="xml">
            <field name="calendar_id" position="after">
                <field
                    name="vehicle_type_ids"
                    widget="many2many_tags"
                    options="{'no_create': True}"
                />
            </field>
        </field>
    </record>
</odoo>
