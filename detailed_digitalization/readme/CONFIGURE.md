The OpenAI API key is shared with the `ai` module: set the `ai.openai_key`
system parameter, or the `ODOO_AI_CHATGPT_TOKEN` environment variable.

Enable "Kleinstunternehmer" on the company record if extracted prices should
be multiplied by 1.2.
