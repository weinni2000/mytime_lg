The target Google Sheet must be shared as "Anyone with the link can view" —
the CSV export is downloaded without authentication. Create a "Guest Tax
Sheet" record with the sheet's URL (including the `gid` of the target tab),
pick a month, and click "Load Month".
